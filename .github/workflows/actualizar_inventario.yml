name: Actualizar inventario desde Odoo

on:
  schedule:
    # Cada hora (minuto 0, UTC)
    - cron: '0 * * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: inventario-horario
  cancel-in-progress: false

jobs:
  actualizar:
    runs-on: ubuntu-latest
    steps:
      - name: Obtener codigo
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Generar inventario desde Odoo (solo lectura)
        env:
          ODOO_USER: ${{ secrets.ODOO_USER }}
          ODOO_PASSWORD: ${{ secrets.ODOO_PASSWORD }}
        run: python generar_inventario.py

      - name: Publicar cambios
        run: |
          git config user.name "latinbienti-sys"
          git config user.email "latinbienti-sys@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "Sin cambios en el inventario."
          else
            git commit -m "Actualizacion automatica del inventario ($(date -u +'%Y-%m-%d %H:%M UTC'))"
            git push origin main
          fi
